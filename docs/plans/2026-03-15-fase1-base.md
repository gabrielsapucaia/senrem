# Fase 1 — Base: FastAPI + Frontend MapLibre

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Criar o esqueleto funcional do SENREM3 — backend FastAPI servindo um frontend com mapa MapLibre GL JS centrado na area de estudo (raio 25km em torno de -11.699153, -47.155531).

**Architecture:** FastAPI serve endpoints REST e arquivos estaticos do frontend. Frontend e uma SPA vanilla (HTML/CSS/JS) com MapLibre GL JS renderizando mapa interativo. Painel lateral com estrutura de layers (sem dados reais ainda). Area de estudo desenhada como circulo no mapa.

**Tech Stack:** Python 3.11+, FastAPI, uvicorn, MapLibre GL JS (CDN), HTML/CSS/JS vanilla

---

### Task 1: Inicializar projeto e dependencias

**Files:**
- Create: `requirements.txt`
- Create: `backend/__init__.py`
- Create: `backend/config.py`

**Step 1: Criar requirements.txt**

```
fastapi==0.115.6
uvicorn[standard]==0.34.0
rasterio==1.4.3
geopandas==1.0.1
shapely==2.0.6
pyproj==3.7.1
numpy==2.2.1
scikit-learn==1.6.1
earthengine-api==1.4.2
requests==2.32.3
httpx==0.28.1
rio-tiler==7.5.1
pydantic==2.10.4
pydantic-settings==2.7.1
```

**Step 2: Criar backend/config.py**

```python
from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    app_name: str = "SENREM3"
    host: str = "0.0.0.0"
    port: int = 8000
    data_dir: str = "data"

    study_area_center_lat: float = -11.699153
    study_area_center_lon: float = -47.155531
    study_area_radius_km: float = 25.0
    study_area_name: str = "Natividade-Almas Greenstone Belt"


settings = Settings()
```

**Step 3: Criar estrutura de diretorios**

Run:
```bash
mkdir -p backend/api backend/services backend/models
mkdir -p frontend
mkdir -p data/rasters data/vectors data/tiles
touch backend/__init__.py backend/api/__init__.py backend/services/__init__.py backend/models/__init__.py
```

**Step 4: Criar venv e instalar dependencias**

Run:
```bash
cd /Users/gabrielsapucaia/Code/senrem3
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Expected: instalacao sem erros

**Step 5: Criar .gitignore**

Create `.gitignore`:
```
.venv/
__pycache__/
*.pyc
data/rasters/*
data/tiles/*
data/vectors/*
!data/rasters/.gitkeep
!data/tiles/.gitkeep
!data/vectors/.gitkeep
.env
*.egg-info/
```

**Step 6: Inicializar git e commit**

Run:
```bash
git init
git add .
git commit -m "chore: inicializar projeto SENREM3 com dependencias e config"
```

---

### Task 2: FastAPI app com endpoint de config

**Files:**
- Create: `backend/main.py`
- Create: `backend/api/config_routes.py`
- Create: `tests/__init__.py`
- Create: `tests/test_config.py`

**Step 1: Escrever teste**

Create `tests/test_config.py`:
```python
from fastapi.testclient import TestClient

from backend.main import app

client = TestClient(app)


def test_get_config():
    response = client.get("/api/config")
    assert response.status_code == 200
    data = response.json()
    assert data["center"]["lat"] == -11.699153
    assert data["center"]["lon"] == -47.155531
    assert data["radius_km"] == 25.0
    assert data["name"] == "Natividade-Almas Greenstone Belt"


def test_health():
    response = client.get("/api/health")
    assert response.status_code == 200
```

**Step 2: Rodar teste para verificar que falha**

Run: `source .venv/bin/activate && python -m pytest tests/test_config.py -v`
Expected: FAIL (modulos nao existem)

**Step 3: Implementar backend/api/config_routes.py**

```python
from fastapi import APIRouter

from backend.config import settings

router = APIRouter(prefix="/api")


@router.get("/config")
def get_config():
    return {
        "center": {
            "lat": settings.study_area_center_lat,
            "lon": settings.study_area_center_lon,
        },
        "radius_km": settings.study_area_radius_km,
        "name": settings.study_area_name,
    }


@router.get("/health")
def health():
    return {"status": "ok"}
```

**Step 4: Implementar backend/main.py**

```python
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles

from backend.api.config_routes import router as config_router

app = FastAPI(title="SENREM3")
app.include_router(config_router)
app.mount("/", StaticFiles(directory="frontend", html=True), name="frontend")


if __name__ == "__main__":
    import uvicorn

    from backend.config import settings

    uvicorn.run("backend.main:app", host=settings.host, port=settings.port, reload=True)
```

**Step 5: Rodar teste para verificar que passa**

Run: `source .venv/bin/activate && python -m pytest tests/test_config.py -v`
Expected: 2 passed

**Step 6: Commit**

```bash
git add backend/ tests/
git commit -m "feat: FastAPI app com endpoint /api/config e /api/health"
```

---

### Task 3: Frontend base com MapLibre GL JS

**Files:**
- Create: `frontend/index.html`
- Create: `frontend/style.css`
- Create: `frontend/app.js`

**Step 1: Criar frontend/index.html**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SENREM3</title>
    <link rel="stylesheet" href="https://unpkg.com/maplibre-gl@4.7.1/dist/maplibre-gl.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header id="app-header">
        <h1>SENREM3</h1>
        <span class="subtitle">Sensoriamento Remoto para Mineracao</span>
    </header>

    <div id="app-container">
        <aside id="sidebar">
            <section id="layers-panel">
                <h2>Layers</h2>
                <div id="layers-list"></div>
            </section>

            <section id="opacity-panel">
                <h2>Opacidade</h2>
                <input type="range" id="opacity-slider" min="0" max="100" value="70">
                <span id="opacity-value">70%</span>
            </section>

            <section id="weights-panel">
                <h2>Pesos</h2>
                <div id="weights-list"></div>
                <button id="btn-generate-targets" disabled>Gerar Alvos</button>
            </section>
        </aside>

        <main id="map-container">
            <div id="map"></div>
        </main>
    </div>

    <footer id="status-bar">
        <span id="status-text">Carregando...</span>
        <span id="cursor-coords"></span>
    </footer>

    <script src="https://unpkg.com/maplibre-gl@4.7.1/dist/maplibre-gl.js"></script>
    <script src="/app.js"></script>
</body>
</html>
```

**Step 2: Criar frontend/style.css**

```css
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    display: flex;
    flex-direction: column;
    height: 100vh;
    background: #1a1a2e;
    color: #e0e0e0;
}

#app-header {
    display: flex;
    align-items: baseline;
    gap: 12px;
    padding: 8px 16px;
    background: #16213e;
    border-bottom: 1px solid #0f3460;
}

#app-header h1 {
    font-size: 18px;
    font-weight: 700;
    color: #e94560;
}

#app-header .subtitle {
    font-size: 12px;
    color: #888;
}

#app-container {
    display: flex;
    flex: 1;
    overflow: hidden;
}

#sidebar {
    width: 280px;
    min-width: 280px;
    background: #16213e;
    border-right: 1px solid #0f3460;
    overflow-y: auto;
    padding: 12px;
    display: flex;
    flex-direction: column;
    gap: 16px;
}

#sidebar h2 {
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: #e94560;
    margin-bottom: 8px;
}

#map-container {
    flex: 1;
    position: relative;
}

#map {
    width: 100%;
    height: 100%;
}

#status-bar {
    display: flex;
    justify-content: space-between;
    padding: 4px 16px;
    background: #16213e;
    border-top: 1px solid #0f3460;
    font-size: 12px;
    color: #888;
}

/* Layer checkboxes */
.layer-item {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 4px 0;
}

.layer-item input[type="checkbox"] {
    accent-color: #e94560;
}

.layer-item label {
    font-size: 13px;
    cursor: pointer;
}

/* Opacity slider */
#opacity-slider {
    width: 100%;
    accent-color: #e94560;
}

#opacity-value {
    font-size: 12px;
    color: #888;
}

/* Weights */
.weight-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 4px 0;
    font-size: 13px;
}

.weight-item input[type="range"] {
    width: 100px;
    accent-color: #e94560;
}

#btn-generate-targets {
    margin-top: 8px;
    padding: 8px 16px;
    background: #e94560;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 13px;
    font-weight: 600;
}

#btn-generate-targets:disabled {
    background: #555;
    cursor: not-allowed;
}

#btn-generate-targets:hover:not(:disabled) {
    background: #c73650;
}

/* Basemap switcher */
.basemap-switcher {
    display: flex;
    gap: 4px;
    margin-bottom: 8px;
}

.basemap-btn {
    padding: 4px 8px;
    background: #0f3460;
    color: #e0e0e0;
    border: 1px solid #1a4a8a;
    border-radius: 3px;
    cursor: pointer;
    font-size: 11px;
}

.basemap-btn.active {
    background: #e94560;
    border-color: #e94560;
}
```

**Step 3: Criar frontend/app.js**

```javascript
const BASEMAPS = {
    satellite: {
        name: "Satelite",
        style: {
            version: 8,
            sources: {
                esri: {
                    type: "raster",
                    tiles: [
                        "https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}"
                    ],
                    tileSize: 256,
                    attribution: "Esri"
                }
            },
            layers: [{ id: "esri-satellite", type: "raster", source: "esri" }]
        }
    },
    topo: {
        name: "Topo",
        style: "https://basemaps.cartocdn.com/gl/voyager-gl-style/style.json"
    },
    dark: {
        name: "Escuro",
        style: "https://basemaps.cartocdn.com/gl/dark-matter-gl-style/style.json"
    }
};

let map;
let studyArea = null;
let currentBasemap = "satellite";

async function init() {
    const config = await fetch("/api/config").then(r => r.json());
    studyArea = config;

    map = new maplibregl.Map({
        container: "map",
        style: BASEMAPS[currentBasemap].style,
        center: [config.center.lon, config.center.lat],
        zoom: 11
    });

    map.addControl(new maplibregl.NavigationControl(), "top-right");
    map.addControl(new maplibregl.ScaleControl({ unit: "metric" }), "bottom-right");

    map.on("load", () => {
        addStudyAreaCircle(config);
        updateStatus("Pronto");
    });

    map.on("mousemove", (e) => {
        const coords = e.lngLat;
        document.getElementById("cursor-coords").textContent =
            `${coords.lat.toFixed(5)}, ${coords.lng.toFixed(5)}`;
    });

    setupBasemapSwitcher();
    setupOpacitySlider();
    setupLayersList();
}

function addStudyAreaCircle(config) {
    const center = [config.center.lon, config.center.lat];
    const radiusKm = config.radius_km;
    const points = 64;
    const coords = [];

    for (let i = 0; i <= points; i++) {
        const angle = (i / points) * 2 * Math.PI;
        const dx = radiusKm * Math.cos(angle);
        const dy = radiusKm * Math.sin(angle);
        const lat = center[1] + (dy / 111.32);
        const lon = center[0] + (dx / (111.32 * Math.cos(center[1] * Math.PI / 180)));
        coords.push([lon, lat]);
    }

    map.addSource("study-area", {
        type: "geojson",
        data: {
            type: "Feature",
            geometry: { type: "Polygon", coordinates: [coords] }
        }
    });

    map.addLayer({
        id: "study-area-fill",
        type: "fill",
        source: "study-area",
        paint: {
            "fill-color": "#e94560",
            "fill-opacity": 0.05
        }
    });

    map.addLayer({
        id: "study-area-outline",
        type: "line",
        source: "study-area",
        paint: {
            "line-color": "#e94560",
            "line-width": 2,
            "line-dasharray": [3, 2]
        }
    });

    // Center marker
    new maplibregl.Marker({ color: "#e94560" })
        .setLngLat(center)
        .setPopup(new maplibregl.Popup().setHTML(
            `<strong>${config.name}</strong><br>` +
            `${config.center.lat}, ${config.center.lon}<br>` +
            `Raio: ${config.radius_km} km`
        ))
        .addTo(map);
}

function setupBasemapSwitcher() {
    const container = document.createElement("div");
    container.className = "basemap-switcher";

    Object.entries(BASEMAPS).forEach(([key, val]) => {
        const btn = document.createElement("button");
        btn.className = "basemap-btn" + (key === currentBasemap ? " active" : "");
        btn.textContent = val.name;
        btn.onclick = () => {
            currentBasemap = key;
            map.setStyle(val.style);
            map.once("style.load", () => {
                if (studyArea) addStudyAreaCircle(studyArea);
            });
            container.querySelectorAll(".basemap-btn").forEach(b => b.classList.remove("active"));
            btn.classList.add("active");
        };
        container.appendChild(btn);
    });

    const layersPanel = document.getElementById("layers-panel");
    layersPanel.insertBefore(container, layersPanel.querySelector("#layers-list"));
}

function setupLayersList() {
    const list = document.getElementById("layers-list");
    const layers = [
        { id: "rgb-true", name: "RGB Verdadeira", available: false },
        { id: "rgb-false", name: "RGB Falsa-cor", available: false },
        { id: "iron-oxide", name: "Oxidos de Ferro", available: false },
        { id: "clay", name: "Argilas / Sericita", available: false },
        { id: "carbonate", name: "Carbonatos", available: false },
        { id: "silica", name: "Silica", available: false },
        { id: "dem", name: "DEM / Hillshade", available: false },
        { id: "lineaments", name: "Lineamentos", available: false },
        { id: "geology", name: "Geologia (CPRM)", available: false },
        { id: "magnetic", name: "Magnetico", available: false },
        { id: "gamma", name: "Gamaespectrometrico", available: false },
        { id: "targets", name: "Alvos", available: false },
    ];

    layers.forEach(layer => {
        const item = document.createElement("div");
        item.className = "layer-item";
        item.innerHTML = `
            <input type="checkbox" id="layer-${layer.id}" disabled>
            <label for="layer-${layer.id}">${layer.name}</label>
        `;
        list.appendChild(item);
    });
}

function setupOpacitySlider() {
    const slider = document.getElementById("opacity-slider");
    const value = document.getElementById("opacity-value");
    slider.addEventListener("input", () => {
        value.textContent = slider.value + "%";
    });
}

function updateStatus(text) {
    document.getElementById("status-text").textContent = "Status: " + text;
}

init();
```

**Step 4: Testar manualmente**

Run:
```bash
source .venv/bin/activate && python -m backend.main
```

Abrir `http://localhost:8000` no browser. Verificar:
- Mapa carrega com imagem de satelite
- Circulo vermelho tracejado de 25km
- Marker no centro com popup
- Painel lateral com layers (desabilitadas)
- Coordenadas do cursor no rodape
- Troca de basemaps funciona

**Step 5: Commit**

```bash
git add frontend/
git commit -m "feat: frontend MapLibre GL JS com mapa, area de estudo e painel lateral"
```

---

### Task 4: Endpoint de layers e estrutura para fases futuras

**Files:**
- Create: `backend/api/layers.py`
- Create: `tests/test_layers.py`

**Step 1: Escrever teste**

Create `tests/test_layers.py`:
```python
from fastapi.testclient import TestClient

from backend.main import app

client = TestClient(app)


def test_list_layers():
    response = client.get("/api/layers")
    assert response.status_code == 200
    data = response.json()
    assert isinstance(data, list)
    assert len(data) > 0
    layer = data[0]
    assert "id" in layer
    assert "name" in layer
    assert "available" in layer
    assert "category" in layer


def test_layers_have_categories():
    response = client.get("/api/layers")
    data = response.json()
    categories = {l["category"] for l in data}
    assert "spectral" in categories
    assert "terrain" in categories
```

**Step 2: Rodar teste para verificar que falha**

Run: `source .venv/bin/activate && python -m pytest tests/test_layers.py -v`
Expected: FAIL

**Step 3: Implementar backend/api/layers.py**

```python
from fastapi import APIRouter

router = APIRouter(prefix="/api")

LAYERS = [
    {"id": "rgb-true", "name": "RGB Verdadeira", "category": "spectral", "available": False, "source": "gee"},
    {"id": "rgb-false", "name": "RGB Falsa-cor", "category": "spectral", "available": False, "source": "gee"},
    {"id": "iron-oxide", "name": "Oxidos de Ferro", "category": "spectral", "available": False, "source": "gee"},
    {"id": "clay", "name": "Argilas / Sericita", "category": "spectral", "available": False, "source": "gee"},
    {"id": "carbonate", "name": "Carbonatos", "category": "spectral", "available": False, "source": "gee"},
    {"id": "silica", "name": "Silica", "category": "spectral", "available": False, "source": "gee"},
    {"id": "dem", "name": "DEM / Hillshade", "category": "terrain", "available": False, "source": "gee"},
    {"id": "lineaments", "name": "Lineamentos", "category": "terrain", "available": False, "source": "local"},
    {"id": "geology", "name": "Geologia (CPRM)", "category": "cprm", "available": False, "source": "cprm"},
    {"id": "magnetic", "name": "Magnetico", "category": "cprm", "available": False, "source": "cprm"},
    {"id": "gamma", "name": "Gamaespectrometrico", "category": "cprm", "available": False, "source": "cprm"},
    {"id": "targets", "name": "Alvos", "category": "prospectivity", "available": False, "source": "model"},
]


@router.get("/layers")
def list_layers():
    return LAYERS
```

**Step 4: Registrar router no main.py**

Adicionar ao `backend/main.py`:
```python
from backend.api.layers import router as layers_router
app.include_router(layers_router)
```

**Step 5: Rodar teste para verificar que passa**

Run: `source .venv/bin/activate && python -m pytest tests/ -v`
Expected: 4 passed

**Step 6: Atualizar app.js para carregar layers da API**

Substituir a funcao `setupLayersList` em `frontend/app.js`:

```javascript
async function setupLayersList() {
    const list = document.getElementById("layers-list");
    const layers = await fetch("/api/layers").then(r => r.json());

    layers.forEach(layer => {
        const item = document.createElement("div");
        item.className = "layer-item";
        item.innerHTML = `
            <input type="checkbox" id="layer-${layer.id}" ${layer.available ? "" : "disabled"}>
            <label for="layer-${layer.id}">${layer.name}</label>
        `;
        list.appendChild(item);
    });
}
```

**Step 7: Commit**

```bash
git add backend/api/layers.py tests/test_layers.py backend/main.py frontend/app.js
git commit -m "feat: endpoint /api/layers com listagem de layers e integracao no frontend"
```

---

### Task 5: Criar .gitkeep e dados de referencia

**Files:**
- Create: `data/rasters/.gitkeep`
- Create: `data/vectors/.gitkeep`
- Create: `data/tiles/.gitkeep`

**Step 1: Criar gitkeeps**

Run:
```bash
touch data/rasters/.gitkeep data/vectors/.gitkeep data/tiles/.gitkeep
```

**Step 2: Commit final da Fase 1**

```bash
git add data/
git commit -m "chore: adicionar diretorios de dados com .gitkeep"
```

**Step 3: Verificar tudo funciona**

Run:
```bash
source .venv/bin/activate && python -m pytest tests/ -v
```

Expected: All tests pass

Run:
```bash
source .venv/bin/activate && python -m backend.main
```

Verificar no browser: mapa funcional, layers listadas, area de estudo visivel.

---

## Resumo da Fase 1

Ao final destas 5 tasks teremos:

- Projeto inicializado com git e dependencias
- FastAPI servindo API + frontend
- Mapa MapLibre centrado na area de estudo com circulo de 25km
- Painel lateral com layers (desabilitadas, prontas para Fase 2)
- Basemaps alternavel (satelite, topo, escuro)
- Coordenadas do cursor em tempo real
- Testes automatizados passando
- Estrutura pronta para integrar GEE na Fase 2
